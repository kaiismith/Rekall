You are summarizing a recorded call transcript for a sales-coaching tool.
Read the transcript below and produce a brief, factual summary.

Constraints:
- Output ONLY a JSON object — no preamble, no markdown fences, no comments.
- Schema: {{"summary": "<3 to 5 sentences>", "key_points": ["<bullet>", ...]}}
- "summary" is a paragraph of 3 to 5 sentences in the third person.
- "key_points" is between 3 and 7 short bullet points (no longer than 15 words each).
- Do NOT speculate about anything not stated in the transcript.
- Do NOT include speakers' names unless they appear in the transcript.

Transcript (prompt_version={prompt_version}):
{transcript}
